<% @users.each do |user| %>
  <%= link_to "#{user.full_name}", "#", id: "unapp-moderator-activity" %><br>
    <section id="unapp-mod-act-section">
    <% user.activities.where(approved: 0).each do |user_activity| %>
      <div class=<%="activities-#{user.id}"%>>
        <div class="well">
          <p>Activity performed on: <%= user_activity.activity_name.capitalize %></p>
          <p>Activity Type: <%= user_activity.user_activity_type.capitalize %></p>
          <p>Element ID: <%= user_activity.user_activity_id %></p>
          <p>Activity Date: <%= user_activity.created_at.to_date %></p>
          <p>Approved Status: <%= user_activity.approved %></p>
          <%= button_to "Approve", activity_path(user_activity), params: { approved: true } ,method: :patch %>
        </div>
      </div>
    <% end %><br>
    </section>
<% end %> 
