$('<%= "#edit-comment-#{@comment.id}" %>').html("<%= j render 'edit' %>");
$('<%= ".comment-links-#{@comment.id}" %>').hide().after('');
$('.new_comment').hide().after();
$('#back-to-blog').show().after();
$('#comment_data').focus();
