<%= form_for [@blog, @blog.comments.build], url: blog_comments_path(@blog), remote: true do |f| %>
  <%= f.label :comment %>
  <%= f.text_area :data, placeholder: 'Write your comment...' %>

  <center><%= f.submit 'Post' %></center>
<% end %>
